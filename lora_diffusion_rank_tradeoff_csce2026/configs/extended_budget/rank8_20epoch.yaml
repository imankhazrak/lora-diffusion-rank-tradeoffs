rank: 8
alpha: 8
dropout: 0.0

lora:
  rank: 8
  alpha: 8
  dropout: 0.0
  target_modules:
    - to_q
    - to_k
    - to_v
    - to_out.0

dataset: cifar10
resolution: 32
batch_size: 64
epochs: 20
learning_rate: 1e-4
mixed_precision: auto
seed: 42

base_model_dir: outputs/smoke/ddpm_cifar10_smoke
output_dir: outputs/extended_budget/rank8_20epoch
